# =============================================================================
# Kustomization - Dev Environment
# =============================================================================
# Dev 환경 전용 리소스 (configmap + secret)
# namespace는 infrastructure/overlays/develop에서 생성
# Prod는 Vault/External Secrets Operator 사용 예정

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - secret.yaml
